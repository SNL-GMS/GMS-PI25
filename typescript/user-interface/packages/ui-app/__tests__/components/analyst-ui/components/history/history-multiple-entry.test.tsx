import { HistoryMultipleEntry } from '~analyst-ui/components/history/history-multiple-entry';

describe('history multiple entry', () => {
  it('exists', () => {
    expect(HistoryMultipleEntry).toBeDefined();
  });
});
